{
  "name": "aws-ssm-secrets-multiple",
  "version": "1.0.0",
  "description": "JavaScript Github Action to get multiple secrets from AWS Systems Manager",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle --platform=node --target=node16 --outfile=dist/index.js src/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tlvince/aws-ssm-secrets-multiple.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript",
    "Webhook",
    "AWS SSM"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tlvince/aws-ssm-secrets-multiple/issues"
  },
  "homepage": "https://github.com/tlvince/aws-ssm-secrets-multiple#readme",
  "engines": {
    "node": ">=16",
    "npm": ">=8"
  },
  "dependencies": {
    "@actions/core": "1.6.0",
    "aws-sdk": "2.1092.0"
  },
  "devDependencies": {
    "esbuild": "0.14.26"
  }
}
